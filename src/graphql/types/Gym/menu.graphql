type Menu {
  _id: ID!
  items: [MenuItem]!
  popular_items: [MenuItem]
}

type MenuItem {
  _id: ID!
  name: String!
  type: MenuItemType!
  price: Int!
  currency: String!
}

enum MenuItemType {
  FOOD
  BEVERAGE
  MISC
}