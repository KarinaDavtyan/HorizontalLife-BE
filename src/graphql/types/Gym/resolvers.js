const resolvers = {
  Gym: {
    routes: gym => gym.routes,
    // menu:  gym => gym.menu,
    // facilities: gym => gym.facilities,
    // timetable: gym => gym.timetable
  },
}

export default resolvers;